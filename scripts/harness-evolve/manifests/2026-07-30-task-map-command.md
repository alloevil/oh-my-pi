# 2026-07-30-task-map-command

- **Component-class**: observability
- **Date**: 2026-07-30
- **Fix**: `packages/coding-agent/src/health/map.ts` (new, pure), `src/slash-commands/builtin-registry.ts` (`/map`), `test/health-map.test.ts`

## Failure evidence

Incident #1 (Mail session `019fac75`): the user watched a live stream of tool
calls with no way to see the trajectory forming — 120 near-identical browser
invocations accumulated before anyone could name the pattern. The declared
plan was no help: todo was called twice in 457 messages, so the only
"task map" in the session was stale from the first hour. Post-hoc, the
`--autopsy` command lineage made the drift obvious in one glance — but only
after the task had already been abandoned to manual takeover.

Design note `docs/notes/2026-07-30-agent-error-discovery-and-task-map.md`
records the analysis: a model-maintained map goes stale (declared-only fails);
a derived map cannot go stale; and the *divergence* between declared and
derived is itself the drift signal users need earliest.

## Root cause

The interactive surface has no trajectory view. Declared intent (todo phases)
and actual behavior (command stream) are both recorded but never juxtaposed;
each is invisible to the other, so their divergence — the earliest deterministic
sign of an agent grinding off-plan — is computed nowhere.

## Targeted fix

Pure `buildTaskMap(entries, todoPhases)` in `src/health/map.ts` over the live
session entries (same shapes the autopsy consumes):

- **declared**: phase progress (done/total per phase, current in-progress
  task), plus turns since the last todo tool call;
- **derived** (trailing window of 30 assistant turns): tool-call counts by
  tool, error count, dominant command prefix with count (live churn counter),
  repetition counters (same path+selector re-reads, byte-identical re-runs) —
  the lost-in-the-middle proxies from the position-resolved measurement;
- **divergence** (deterministic, display-only): flagged when todos exist but
  the last todo update is ≥15 commands old ("declared plan may be stale"), or
  when no todos exist after ≥20 assistant turns ("no declared plan").

`/map` slash command beside `/health` (same `runtime.output` idiom):
autocomplete shows a one-line summary, invocation prints the map. Display
only; no ledger writes, no thresholds beyond the divergence heuristics, which
judge staleness of *declarations*, not quality of behavior.

## Prediction

- routing canary ≥ 87.4% (5pp tolerance of 92.4%); edit gate ≥ 27/30 (1-task
  tolerance of 28/30) — observability-only.
- Replaying the incident session's entries through `buildTaskMap` at its final
  state: derived shows `ego-browser nodejs <<'EOF'` as dominant prefix with
  count ≥ 50 in-window or lifetime ≥ 100, repetition counters non-zero, and
  the divergence line fires (todo stale).
- On this dev session's entries (long, todo-active): divergence does NOT fire
  at states where a todo call happened within the last 15 commands.
- Unit contracts: declared progress arithmetic, window clipping, dominant
  prefix tie-breaking, both divergence branches (stale vs absent), determinism.
- `/map` appears in the builtin registry with autocomplete text (asserted by
  test or smoke).

## Verification (auto)

_Generated by `bun run manifest:verify` at 2026-08-02T05:07:42.233Z. Do not edit — re-runs replace this whole section._

| Metric | Measured | Baseline | Delta | Gate | Exit code |
|---|---|---|---|---|---|
| Routing canary (top-1) | 93.1% | 92.4% | +0.7pp | PASS | 0 |
| Edit gate | 27/30 | 28/30 | -1 task(s) | PASS | 0 |

Manual metrics — predictions the canary does not measure; fill by hand:

- [x] (measured: dominant `ego-browser nodejs <<'EOF'` ×18 in-window / 120 lifetime; repetition 7 re-reads + 3 identical re-runs; both divergence branches fire — 'no declared plan after 226 turns' and, with a reconstructed phase, 'stale: 165 commands since last todo update') Replaying the incident session's entries through `buildTaskMap` at its final state: derived shows `ego-browser nodejs <<'EOF'` as dominant prefix with count ≥ 50 in-window or lifetime ≥ 100, repetition counters non-zero, and the divergence line fires (todo stale).
- [x] (unit contract: divergence quiet at 14 commands since todo, fires at 15) On this dev session's entries (long, todo-active): divergence does NOT fire at states where a todo call happened within the last 15 commands.
- [x] (7/7: progress arithmetic, window clipping, dominant tie-break + lifetime, repetition seeding against pre-window history, both divergence branches, determinism) Unit contracts: declared progress arithmetic, window clipping, dominant prefix tie-breaking, both divergence branches (stale vs absent), determinism.
- [x] (registered beside /health in builtin-registry with autocomplete summary; typecheck-verified) `/map` appears in the builtin registry with autocomplete text (asserted by test or smoke).

### Addendum 2026-08-02 — task regression (user report)

User observation: within multi-task sessions the agent sometimes re-executes
an already-completed task. Probe (command-cluster resurrection: prefix dormant
≥30 turns then active again) FAILED as a detector — 29 hits in one dev session,
all benign (git checkout / test / format recur by nature); task identity is
semantic and lexical clustering cannot see it. Recorded in incidents.md.
Deterministic detection is therefore out; the map instead arms the human:
`declared` now lists the last 3 completed task names (`✓ recently completed`),
so current activity can be checked against the done-list at a glance. Display
only; 2 new contract tests; no gate re-run (pure render addition inside this
card's already-verified feature — the gates measure routing/edit, which this
cannot move; re-running would sample provider noise, not the change).

### Addendum 2026-08-02 (2) — render clarity revision (user feedback)

User: the map's content wasn't clear. Diagnosis: engineering vocabulary leaked
into the UI (declared/derived/divergence, "dominant prefix") and every line
carried equal visual weight. Render-only revision, builder untouched: sections
renamed PLAN / ACTIVITY, per-phase ten-cell progress bars, current task as
`▸ now:`, done-list as `✓ done:`, "hottest command" with a ≥3 render floor (a
hottest-of-one is normal texture and would teach users to ignore the line),
errors flagged ⚠ only when nonzero, and the verdict line moved last —
`✓ plan and activity agree` / `⚠ plan may be stale…`. Divergence message
wording simplified to match. 9/9 contract tests updated and green; no gate
re-run (pure render text within this card's verified feature).

### Note on a first-run gate failure

The first manifest:verify run scored the edit gate 26/30 (FAIL). The change is
display-only and cannot touch the edit path; an isolated gate re-run scored
29/30 and the recorded verification run passed. Attributed to provider noise —
same failure mode as the 25/30 brownout observed during gate calibration.
Recorded rather than erased: single-run gates on a brokered provider carry
roughly a one-task noise floor, which is why the tolerance exists.

## Verdict

keep — user-approved 2026-08-02; incident replay matched all predictions; first-run gate noise event recorded
